# 更改串口CRC算法计划

## 1. 替换CRC算法函数

* 替换`crc16_ccitt_false`函数为`bin_complement_check_sum`函数
* 新算法实现：计算所有字节的和，然后返回其补码加1

## 2. 更新CRC相关常量

* 将`SOC_MCU_USE_CRC16`常量修改为0，因为新算法是8位校验和
* 确保`SOC_MCU_CRC_LEN`正确设置为1

## 3. 修改帧构建函数

* 在`SocMcu_Frame_Build`函数中，更新CRC计算逻辑
* 使用新的`bin_complement_check_sum`函数计算校验和
* 确保校验和正确添加到帧中

## 4. 修改帧解析函数

* 在`SocMcu_Frame_Parse`函数中，更新CRC验证逻辑
* 使用新的`bin_complement_check_sum`函数验证校验和
* 确保校验和验证正确

## 5. 重新打包应用程序

* 运行setup.py脚本重新打包
* 验证生成的exe文件能正常运行

## 实现步骤

1. 修改`protocol/uart_protocol.py`文件，替换CRC算法函数
2. 更新CRC相关常量
3. 修改帧构建和解析函数中的CRC逻辑
4. 运行setup.py脚本重新打包应用程序
5. 测试生成的exe文件